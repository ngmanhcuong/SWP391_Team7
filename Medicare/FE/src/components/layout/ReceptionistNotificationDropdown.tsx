import React from 'react';
import AppNotificationDropdown from './AppNotificationDropdown';

const ReceptionistNotificationDropdown: React.FC = () => (
  <AppNotificationDropdown footerHref="/receptionist/thong-bao" footerLabel="Xem tất cả thông báo" />
);

export default ReceptionistNotificationDropdown;
