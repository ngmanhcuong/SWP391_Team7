import React from 'react';
import AppNotificationDropdown from './AppNotificationDropdown';

const PatientNotificationDropdown: React.FC = () => (
  <AppNotificationDropdown footerHref="/patient/thong-bao" footerLabel="Xem tất cả thông báo" />
);

export default PatientNotificationDropdown;
